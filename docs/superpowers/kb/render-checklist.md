# Render Checklist

- Confirm the task really needed a render artifact.
- Keep the route inside Pilot-governed execution.
- Prefer a `.scd` source path for `sc-audio-generation` tasks.
- Verify the render artifact exists and is non-empty.
- Record a `summarize_session` result before closure.
- Add an explicit review note before promotion-style decisions.
